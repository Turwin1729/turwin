version: '3.8'

services:
  api1:
    build: .
    expose:
      - "1730"
    volumes:
      - ./hospital.db:/app/hospital.db
      - ./uploads:/app/uploads
    environment:
      - FLASK_ENV=production
    restart: unless-stopped

  api2:
    build: .
    expose:
      - "1730"
    volumes:
      - ./hospital.db:/app/hospital.db
      - ./uploads:/app/uploads
    environment:
      - FLASK_ENV=production
    restart: unless-stopped

  api3:
    build: .
    expose:
      - "1730"
    volumes:
      - ./hospital.db:/app/hospital.db
      - ./uploads:/app/uploads
    environment:
      - FLASK_ENV=production
    restart: unless-stopped

  frontend:
    build: ./frontend
    expose:
      - "3000"
    environment:
      - REACT_APP_API_URL=https://api1.turwin.tahabiyikli.com
    restart: unless-stopped

  nginx:
    image: nginx:latest
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
      - ./ssl:/etc/nginx/ssl:ro
      - ./frontend/build:/usr/share/nginx/html:ro
    depends_on:
      - api1
      - api2
      - api3
      - frontend
    restart: unless-stopped
